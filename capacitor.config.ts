import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.image.crop',
  appName: 'cs-image-crop',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
